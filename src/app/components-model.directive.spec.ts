import { ComponentsModelDirective } from './components-model.directive';

describe('ComponentsModelDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentsModelDirective();
    expect(directive).toBeTruthy();
  });
});
